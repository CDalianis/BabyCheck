services:
  postgres:
    image: postgres:16-alpine
    container_name: babycheck-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-babycheck}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-babycheck}
      POSTGRES_DB: ${POSTGRES_DB:-babycheck}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-babycheck}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
